<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Truth Social Parser</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <h1>🔥 Truth Social Parser</h1>
        
        <div class="status-panel">
            <div class="status-item">
                <span class="label">Status:</span>
                <span id="parser-status" class="status <%= stats.isRunning ? 'running' : 'stopped' %>">
                    <%= stats.isRunning ? 'Running' : 'Stopped' %>
                </span>
            </div>
            <div class="status-item">
                <span class="label">Total Posts:</span>
                <span id="total-posts"><%= stats.totalPosts %></span>
            </div>
            <div class="status-item">
                <span class="label">Errors:</span>
                <span id="total-errors"><%= stats.errors %></span>
            </div>
        </div>

        <div class="controls">
            <button id="start-btn" class="btn btn-success">Start Parser</button>
            <button id="stop-btn" class="btn btn-danger">Stop Parser</button>
        </div>

        <div class="section">
            <h2>📋 Profiles to Monitor</h2>
            <div class="add-profile">
                <input type="text" id="username-input" placeholder="Username (e.g., realDonaldTrump)">
                <input type="text" id="keywords-input" placeholder="Keywords (comma separated)">
                <button id="add-profile-btn" class="btn btn-primary">Add Profile</button>
            </div>
            <div id="profiles-list" class="profiles-list"></div>
        </div>


<div class="section">
            <h2>👥 Account Management</h2>
            
            <!-- Статус-панель требований -->
            <div class="status-panel">
                <div class="status-item">
                    <span class="status-label">Profiles:</span>
                    <span id="profiles-count-status" class="status-value">0</span>
                </div>
                <div class="status-item">
                    <span class="status-label">Authorized Accounts:</span>
                    <span id="authorized-count-status" class="status-value">0</span>
                </div>
                <div class="status-item">
                    <span class="status-label">Required Accounts:</span>
                    <span id="required-count-status" class="status-value">0</span>
                </div>
                <div class="status-item">
                    <span class="status-label">Still Need:</span>
                    <span id="still-need-status" class="status-value status-warning">0</span>
                </div>
                <div class="status-item">
                    <span class="status-label">Readiness:</span>
                    <span id="readiness-status" class="status-badge status-error">Not Ready</span>
                </div>
            </div>
      <div class="info-panel">
                <div class="info-item">
                    <span class="info-icon">📋</span>
                    <span class="info-text"><strong>NEW RULE:</strong> 1 profile = 10 accounts (for ultra-fast parsing)</span>
                </div>
                <div class="info-item">
                    <span class="info-icon">⚡</span>
                    <span class="info-text">Each browser runs <strong>max 2 tabs</strong> for maximum stability</span>
                </div>
                <div class="info-item">
                    <span class="info-icon">🎯</span>
                    <span class="info-text">Example: 1 profile needs <strong>10 authorized accounts</strong> (10 browsers × 2 tabs)</span>
                </div>
                <div class="info-item">
                    <span class="info-icon">🚀</span>
                    <span class="info-text">Result: <strong>Ultra-fast</strong> post detection with maximum stability</span>
                </div>
            </div>
            <div class="add-account">
                <input type="text" id="account-username-input" placeholder="Account username (e.g., myaccount)">
                <button id="add-account-btn" class="btn btn-primary">Add Account</button>
            </div>
            <div id="accounts-list" class="accounts-list"></div>
        </div>

        <div class="section">
            <h2>📊 Statistics</h2>
            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-number" id="profiles-count">0</div>
                    <div class="stat-label">Profiles Monitored</div>
                </div>
                <div class="stat-card">
                    <div class="stat-number" id="avg-parse-time">0ms</div>
                    <div class="stat-label">Average Parse Time</div>
                </div>
                <div class="stat-card">
                    <div class="stat-number" id="min-parse-time">0ms</div>
                    <div class="stat-label">Best Time</div>
                </div>
                <div class="stat-card">
                    <div class="stat-number" id="max-parse-time">0ms</div>
                    <div class="stat-label">Worst Time</div>
                </div>
            </div>
        </div>

        <div class="section">
            <h2>📝 Recent Posts</h2>
            <div class="controls">
                <button id="clear-posts-btn" class="btn btn-danger">Clear Posts</button>
            </div>
            <div id="recent-posts" class="posts-container"></div>
        </div>

        <div class="section">
            <h2>🔧 Live Logs</h2>
            <div class="controls">
                <button id="clear-logs-btn" class="btn btn-danger">Clear Logs</button>
            </div>
            <div id="logs-container" class="logs-container"></div>
            <div id="logs-container" class="logs-container"></div>
        </div>
    </div>

    <script src="/socket.io/socket.io.js"></script>
    <script src="/script.js"></script>
</body>
</html>