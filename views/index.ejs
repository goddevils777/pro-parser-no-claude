<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - <%= version %></title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <h1>🔥 Truth Social Parser - API Version</h1>
        
        <div class="mode-banner">
            <span class="mode-icon">⚡</span>
            <span class="mode-text">API Mode Active - Browsers Disabled</span>
            <span class="mode-desc">Using HTTP requests instead of browser automation</span>
        </div>
        
        <div class="status-panel">
            <div class="status-item">
                <span class="label">Status:</span>
                <span id="parser-status" class="status stopped">API Ready</span>
            </div>
            <div class="status-item">
                <span class="label">Mode:</span>
                <span id="parser-mode" class="status api">API-Only</span>
            </div>
            <div class="status-item">
                <span class="label">Total Posts:</span>
                <span id="total-posts">0</span>
            </div>
            <div class="status-item">
                <span class="label">Profiles Monitored:</span>
                <span id="profiles-count">0</span>
            </div>
        </div>

        <div class="section">
            <h2>🔐 Truth Social Authorization</h2>
            <div class="auth-status" id="auth-status">
                <span class="auth-label">Status:</span>
                <span id="auth-status-text" class="status stopped">Not Authorized</span>
            </div>
            <div class="auth-info">
                <div class="info-item">
                    <span class="info-icon">💡</span>
                    <span class="info-text"><strong>Easy Setup:</strong> Get your Bearer token from Truth Social web app (F12 → Network → any API request → Authorization header)</span>
                </div>
            </div>
            <div class="auth-form">
                <input type="text" id="auth-token" placeholder="Bearer token (starts with 'ey...')">
                <button id="auth-token-btn" class="btn btn-primary">Set Token</button>
            </div>
        </div>

        <div class="controls">
            <button id="start-monitoring-btn" class="btn btn-success">Start API Monitoring</button>
            <button id="stop-monitoring-btn" class="btn btn-danger" disabled>Stop Monitoring</button>
            <button id="test-truth-social-btn" class="btn btn-info">Test IP Connection</button>
        </div>

        <div class="section">
            <h2>📋 Profiles to Monitor</h2>
            <div class="add-profile">
                <input type="text" id="username-input" placeholder="Username (e.g., realDonaldTrump)">
                <input type="text" id="keywords-input" placeholder="Keywords (comma separated, optional)">
                <button id="add-profile-btn" class="btn btn-primary">Add Profile</button>
            </div>
            <div id="profiles-list" class="profiles-list"></div>
        </div>

        <div class="section">
            <h2>🔧 API Information</h2>
            <div class="api-info">
                <div class="info-item">
                    <span class="info-icon">📡</span>
                    <span class="info-text"><strong>HTTP Requests:</strong> Direct API calls to Truth Social</span>
                </div>
                <div class="info-item">
                    <span class="info-icon">⚡</span>
                    <span class="info-text"><strong>Faster Setup:</strong> No browser dependencies or complex authorization</span>
                </div>
                <div class="info-item">
                    <span class="info-icon">🔒</span>
                    <span class="info-text"><strong>Lightweight:</strong> Uses standard HTTP requests with User-Agent rotation</span>
                </div>
                <div class="info-item">
                    <span class="info-icon">🎯</span>
                    <span class="info-text"><strong>Rate Limiting:</strong> Built-in request throttling to avoid blocking</span>
                </div>
            </div>
        </div>

        <div class="section">
            <h2>📊 API Statistics</h2>
            <div class="stats-grid">
                <div class="stat-item">
                    <span class="stat-label">API Requests:</span>
                    <span id="api-requests-count" class="stat-value">0</span>
                </div>
                <div class="stat-item">
                    <span class="stat-label">Success Rate:</span>
                    <span id="success-rate" class="stat-value">-</span>
                </div>
                <div class="stat-item">
                    <span class="stat-label">Last Check:</span>
                    <span id="last-check-time" class="stat-value">-</span>
                </div>
                <div class="stat-item">
                    <span class="stat-label">Response Time:</span>
                    <span id="avg-response-time" class="stat-value">-</span>
                </div>
            </div>
        </div>

        <div class="section">
            <h2>📝 Recent Posts</h2>
            <div class="controls">
                <button id="clear-posts-btn" class="btn btn-danger">Clear Posts</button>
            </div>
            <div id="recent-posts" class="posts-container"></div>
        </div>

        <div class="section">
            <h2>🔧 Live Logs</h2>
            <div class="controls">
                <button id="clear-logs-btn" class="btn btn-danger">Clear Logs</button>
            </div>
            <div id="logs-container" class="logs-container"></div>
        </div>
    </div>

    <script src="/socket.io/socket.io.js"></script>
    <script src="/script.js"></script>
</body>
</html>